import { createStore } from "vuex";
import ModalMap from './ModalMap';

export default createStore({
    state: {},
    getters: {},
    mutations: {},
    actions: {},
    modules:{
        ModalMap,
    }
});